bundle:
  name: kingfisher-wells-streamlit

resources:
  apps:
    kingfisher_streamlit_app:
      name: kingfisher-streamlit-app
      source_code_path: src/app
      description: "Kingfisher County surface well comparison app (Streamlit)"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-6841fdbc-f68b.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-6841fdbc-f68b.cloud.databricks.com
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
